from __future__ import annotations


class SingleLoopStrategy:
    name = "Single-loop learning"
    loop_type = "single_loop"

    def adapter_text(self) -> str:
        return "Capture the immediate corrective lesson that prevents the same local miss without changing the operating model."
